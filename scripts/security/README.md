# Security checks

Security checks are structural and secret-safe by default. They inspect path
ownership, nested Git metadata, generated evidence boundaries and provenance
metadata without printing credential values or prompt/response bodies.
