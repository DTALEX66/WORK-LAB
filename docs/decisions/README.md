# Shared contracts

Only stable, versioned cross-module schemas belong here. Implementations remain in
their owning module. A contract change requires compatibility fixtures and an
integration test; source code must not be copied between modules.
