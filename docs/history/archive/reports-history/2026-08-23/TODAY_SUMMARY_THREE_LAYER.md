# 今日总结 + 三层结构重新审计（2026-08-23）

> 用户最终定义：全局=跨软件跨项目通用规则；项目=各项目内部；软件=各软件配置。

## 一、今日工作脉络

1. 识别 Hermes 桌面警告截图（DeepSeek 视觉）→ 发现桌面构建过旧
2. Hermes 桌面修复（官方更新 + 覆盖，最终 stamp 与代码一致）
3. 五维基线①修正：唯一入口 = 官方标准格式
4. 确立硬规则：软件更新以官方为准，禁私自本地打包
5. 全软件五维基线审计（Hermes/Codex/CC Switch/OpenHuman/Open Design/gh/DSH）
6. 任务 1/2/3/4/9：soak + CI 全绿 + Ollama 修复 + 收口 + 路由建议
7. Ollama 官方安装 + 模型路径保持 Model library
8. 并行协作调研 → 通用并行框架（路径所有权 + worktree + 审计合并）
9. 多轮审计 + 纠正：全局 = 所有软件（非 Hermes 单独）
10. 最终三层结构澄清

## 二、三层结构（最终正确理解）

```
① 全局层（一套，所有软件 + 所有项目共同遵守）
   通用规则：安全边界（E盘禁访）、数据边界（不外溢/漂移）、
   官方更新优先、五维基线、共用库规则

② 项目层（各项目内部独立）
   WORK-LAB 工作流规则/13 skills；ArcheAxis 知识规则；DESIGN-LAB 设计规则

③ 软件层（各软件自身配置）
   Hermes config/skills；Codex config/skills；DSH settings
```

## 三、重新审计结论（按三层）

### ① 全局层 —— 核心问题：规则存在但【分散未统一】

| 检查项 | 现状 |
|---|---|
| E盘禁访 | WORK-LAB 有（AGENTS.md:38）+ ArcheAxis 有（AGENTS.md:42）——**各写各的，重复不统一** |
| DESIGN-LAB | 无 AGENTS.md → **无全局规则** |
| 各软件引用 | Codex config 仅 1 处弱引用，其余软件**不引用** |
| 单一规则源 | 无——散在 AGENTS.md + 用户记忆 + 报告 |

→ 缺：**单一全局规则源**，一套部署到三项目 + 所有软件

### ② 项目层 —— 各项目独立（合理），但 DESIGN-LAB 缺规则 + 6MB 副本

### ③ 软件层 —— Hermes 82 skills + Codex 328 skills 膨胀，执行不调用

## 四、核心待办（按三层）

| 层 | 待办 |
|---|---|
| ① 全局 | 建单一规则源（精简通用规则）→ 部署三项目 + 所有软件 |
| ② 项目 | DESIGN-LAB 补规则 + 清理 6MB 副本；ArcheAxis CI 分层 |
| ③ 软件 | Hermes 82→~25 + Codex 328→~20 技能精简 + 强制调用机制 |
| 横切 | 并行框架落地（替代单写者）+ 审计分层 |

## 五、状态

- 三层结构已对齐
- 全局层重新审计完成（分散未统一是根因）
- 待用户确认执行顺序