{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "WORK-LAB Observer default capabilities (read-only + window drag for borderless shell)",
  "windows": [
    "main",
    "panel"
  ],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging"
  ]
}
