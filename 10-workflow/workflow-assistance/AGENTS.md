# Module instructions

This module owns client-neutral workflow governance, execution control and
observability. Keep implementation here; publish only stable contracts at the
root. Cross-module edits require an explicit cross task card. Never include
credentials, prompt/response bodies or provider tokens in evidence.
