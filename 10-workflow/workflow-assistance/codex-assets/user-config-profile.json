{
  "schemaVersion": "workflow/codex-user-config-profile/v1",
  "generatedAt": "2026-08-20",
  "scope": "Codex USER_OVERLAY fields (config-ownership.json: codex layer=USER_OVERLAY mode=MANAGE preserve_unknown=true)",
  "principle": "Official baseline stays untouched (Codex defaults); this profile restores ONLY declared user fields after version updates/resets. Unknown fields are never touched (preserve_unknown).",
  "fields": {
    "desktop.followUpQueueMode": "queue",
    "desktop.appearanceTheme": "dark",
    "desktop.conversationDetailMode": "STEPS_COMMANDS",
    "windows.sandbox": "elevated"
  },
  "provider": {
    "modelProvider": "custom",
    "note": "custom = native OpenAI direct (requires_openai_auth, no base_url). 2026-08-20 moved off cc-switch proxy (stale OAuth + 502).",
    "model": "gpt-5.6-terra"
  }
}
